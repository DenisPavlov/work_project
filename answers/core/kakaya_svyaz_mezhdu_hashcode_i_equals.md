Объекты равны, когда <code>a.equals(b) == true</code> и <code>a.hashCode == b.hashcode == true</code>. Но необязательно, чтобы два различных объекта возвращали различные хэш коды(такая ситуация называется коллизией).

