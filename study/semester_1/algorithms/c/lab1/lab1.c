#include <stdio.h>
main() {
    int arr[3];
    arr[0] = 11;
    arr[1] = 222;
    arr[2] = 3333;
    for(int i = 0; i < sizeof(arr); i++) {
        //определить номер наименьшего элемента в подмассиве от i до max
        // поменять текущий с наименьшим
    }
}
